pipeline:
  generate-tags:
    image: quay.io/natlibfi/drone-gen-tags
  docker:
    image: plugins/docker
    registry: quay.io
    repo: quay.io/natlibfi/melinda-yazproxy
    secrets:
    - docker_username
    - docker_password
    when:
      event: [push]
      branch: [master, test]
